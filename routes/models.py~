from django.db import models

# Create your models here.
class Student(models.Model):
    name = models.CharField(max=50)
    pin = models.CharField(max=13)
    age = models.IntegerField()
    department = models.CharField(max=50)
    
